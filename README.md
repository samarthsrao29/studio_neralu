# Studio Neralu (static site)

Static, single-page architecture website.

## Run locally

Open `index.html` in your browser.

## Customize

- Replace contact info in `index.html`
- Swap placeholder project images in `index.html` (look for “Selected work”)
- Update colors + typography in `assets/css/style.css`

